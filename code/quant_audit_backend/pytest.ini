[pytest]
testpaths = tests
addopts = -q --strict-markers -m "not live"
markers =
    live: hits the real yfinance API; deselected by default, run with `pytest -m live`
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
