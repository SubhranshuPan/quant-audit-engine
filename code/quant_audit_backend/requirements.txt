# Runtime dependencies only. statsmodels, scipy, pytest and httpx are
# development dependencies -- the shipped app never imports them (the HAC
# estimator is pure NumPy on purpose, and the p-value uses math.erfc rather
# than scipy). Keeping them out of the image matters on a small instance.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
numpy>=1.26
pandas>=2.2
yfinance>=0.2.40
python-multipart>=0.0.9
